[project]
name = "probe-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.102.0",
    "click>=8.4.0",
    "fastmcp>=3.3.1",
    "rich>=15.0.0",
]
